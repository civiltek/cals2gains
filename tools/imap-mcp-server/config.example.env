# === IMAP (lectura) ===
IMAP_HOST=mail.cals2gains.com
IMAP_PORT=993
IMAP_USE_SSL=true

# === SMTP (envío) ===
SMTP_HOST=mail.cals2gains.com
SMTP_PORT=465
SMTP_USE_SSL=true

# === Credenciales ===
MAIL_USERNAME=info@cals2gains.com
MAIL_PASSWORD=REEMPLAZA_CON_LA_PASSWORD_DEL_WEBMAIL
MAIL_FROM_NAME=Cals2Gains

# === Opcional ===
DEFAULT_FOLDER=INBOX
